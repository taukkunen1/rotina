id-template-columns:repeat(6,1fr);
    gap:6px;
    margin:4px 0 8px;
  }
  .pet-milestone{
    border:1px solid var(--line);
    border-radius:7px;
    padding:5px 4px;
    text-align:center;
    font-size:.58rem;
    color:var(--chalk-dim);
    background:rgba(255,255,255,.025);
  }
  .pet-milestone.active{
    color:#16241f;
    background:var(--green);
    border-color:var(--green);
    font-weight:bold;
  }
  .pet-milestone.current{
    outline:2px solid var(--yellow);
    outline-offset:1px;
  }
  .pet-milestone small{
    display:block;
    margin-top:2px;
    opacity:.75;
    font-size:.92em;
  }
  @media(max-width:520px){
    .pet-milestones{ grid-template-columns:repeat(3,1fr); }
  }

  .pet-caption{
    font-size:.8rem;
    color:var(--chalk-dim);
    text-align:center;
    max-width:420px;
    line-height:1.4;
  }
  .pet-caption b{ color:var(--green); }

  .pet-evolved-banner{
    background:rgba(255,217,61,.12);
    border:1px solid rgba(255,217,61,.4);
    color:var(--yellow);
    font-size:.82rem;
    padding:8px 14px;
    border-radius:8px;
    margin-top:10px;
    text-align:center;
    animation: popIn .3s ease;
  }
  @keyframes popIn{
    from{ transform:scale(.85); opacity:0; }
    to{ transform:scale(1); opacity:1; }
  }

  /* ---------- crescimento diário do Pacus ---------- */
  .pet-day-progress{
    width:min(520px, 100%);
    margin:6px 0 2px;
  }
  .pet-day-progress-head{
    display:flex;
    justify-content:space-between;
    gap:10px;
    font-size:.68rem;
    color:var(--chalk-dim);
    margin-bottom:5px;
  }
  .pet-day-progress-head b{ color:var(--green); }
  .pet-day-track{
    height:7px;
    border-radius:999px;
    overflow:hidden;
    background:rgba(255,255,255,.08);
    border:1px solid var(--line);
  }
  .pet-day-fill{
    height:100%;
    width:0;
    background:var(--blue);
    border-radius:999px;
    transition:width .7s cubic-bezier(.2,.8,.2,1);
  }
  .pet-mascot.pet-egg{
    transform-origin:50% 70%;
    animation:eggLife 2.8s ease-in-out infinite;
  }
  .pet-mascot.pet-growth-burst{
    animation:petGrowthBurst .75s cubic-bezier(.2,.8,.2,1);
  }
  .pet-egg .egg-crack{
    transition:opacity .5s ease, stroke-width .5s ease;
  }
  @keyframes eggLife{
    0%,100%{ transform:translateY(0) rotate(0deg); }
    50%{ transform:translateY(-2px) rotate(.7deg); }
  }
  @keyframes petGrowthBurst{
    0%{ transform:scale(1) rotate(0deg); }
    25%{ transform:scale(1.08) rotate(-3deg); }
    50%{ transform:scale(.96) rotate(3deg); }
    75%{ transform:scale(1.035) rotate(-1deg); }
    100%{ transform:scale(1) rotate(0deg); }
  }
  .pet-hatch-crack{ animation:crackFlash .55s ease-out; }
  @keyframes crackFlash{
    0%{ filter:brightness(1); }
    35%{ filter:brightness(1.28); }
    100%{ filter:brightness(1); }
  }

  /* ---------- animações contínuas do Pacus (todo dia, o tempo todo) ---------- */
  .pacus-idle{
    animation:pacusBob 3.4s ease-in-out infinite;
  }
  @keyframes pacusBob{
    0%,100%{ transform:translateY(0); }
    50%{ transform:translateY(-3px); }
  }
  .pacus-eye{
    animation:pacusBlink 5.2s ease-in-out infinite;
  }
  @keyframes pacusBlink{
    0%,90%,100%{ transform:scaleY(1); }
    94%{ transform:scaleY(.12); }
  }
  .pacus-gill{
    animation:pacusGillSway 2.4s ease-in-out infinite;
  }
  @keyframes pacusGillSway{
    0%,100%{ transform:rotate(-5deg); }
    50%{ transform:rotate(5deg); }
  }
  .pacus-bubble{
    animation:pacusBubbleRise 3.2s ease-in infinite;
  }
  @keyframes pacusBubbleRise{
    0%{ transform:translateY(0); opacity:.55; }
    85%{ opacity:.25; }
    100%{ transform:translateY(-42px); opacity:0; }
  }

  .streak-strip{ margin-top:14px; text-align:center; }
  .streak-title{
    font-size:.65rem;
    text-transform:uppercase;
    letter-spacing:1px;
    color:var(--chalk-dim);
    margin-bottom:6px;
  }
  .streak-row{ display:flex; gap:6px; justify-content:center; }
  .streak-cell{
    width:38px; height:30px;
    border-radius:7px;
    display:flex;
    align-items:center;
    justify-content:center;
    border:1px solid var(--line);
    background:rgba(255,255,255,.03);
  }
  .streak-letter{ font-size:.56rem; color:var(--chalk-dim); letter-spacing:.2px; }
  .streak-cell.perfect{ background:var(--green); border-color:var(--green); }
  .streak-cell.perfect .streak-letter{ color:#132015; font-weight:bold; }
  .streak-cell.almost{ background:rgba(79,195,247,.32); border-color:var(--blue); }
  .streak-cell.almost .streak-letter{ color:#0f2733; font-weight:bold; }
  .streak-cell.partial{ background:rgba(255,217,61,.35); border-color:var(--yellow); }
  .streak-cell.partial .streak-letter{ color:#2a2410; font-weight:bold; }
  .streak-cell.none{ background:rgba(255,107,107,.18); border-color:rgba(255,107,107,.4); }
  .streak-cell.today{ box-shadow:0 0 0 2px var(--yellow); }

  /* ---------- períodos ---------- */
  .periods{
    display:grid;
    grid-template-columns:repeat(3, 1fr);
    gap:0;
    border:1px solid var(--line);
    border-radius:10px;
    overflow:hidden;
  }
  @media (max-width:760px){
    .periods{ grid-template-columns:1fr; }
  }

  .period{
    background:rgba(255,255,255,.015);
    padding:16px 16px 14px;
  }
  .period:not(:last-child){ border-right:1px dashed var(--line); }
  @media (max-width:760px){
    .period:not(:last-child){ border-right:none; border-bottom:1px dashed var(--line); }
  }

  .period-head{
    display:flex;
    align-items:baseline;
    justify-content:space-between;
    margin-bottom:2px;
  }
  .period h2{
    font-family:"Segoe Print","Bradley Hand",cursive;
    font-weight:normal;
    font-size:1.3rem;
    margin:0;
  }
  .period .time{
    font-size:.68rem;
    color:var(--chalk-dim);
  }
  .period .progress-txt{
    font-size:.68rem;
    color:var(--chalk-dim);
    margin-bottom:10px;
    display:block;
  }

  .time-remaining{
    font-size:.66rem;
    display:block;
    margin-bottom:4px;
    font-weight:bold;
  }
  .time-remaining.urgency-none{ color:var(--chalk-dim); font-weight:normal; }
  .time-remaining.urgency-low{ color:var(--chalk-dim); }
  .time-remaining.urgency-medium{ color:var(--orange); }
  .time-remaining.urgency-high{ color:var(--red); }
  .time-remaining.urgency-over{ color:var(--chalk-dim); font-style:italic; font-weight:normal; }

  .manha h2{ color:var(--yellow); }
  .tarde h2{ color:var(--orange); }
  .noite h2{ color:var(--blue); }

  .progress-bar{
    height:4px;
    border-radius:2px;
    background:rgba(255,255,255,.08);
    margin:6px 0 12px;
    overflow:hidden;
  }
  .progress-bar > div{
    height:100%;
    background:var(--green);
    transition:width .25s ease;
  }

  .next-period-preview{
    background:rgba(79,195,247,.08);
    border:1px dashed rgba(79,195,247,.4);
    border-radius:8px;
    padding:7px 10px;
    font-size:.76rem;
    color:var(--chalk);
    margin-bottom:12px;
    animation:detailsFadeIn .3s ease;
  }
  .next-period-preview b{ color:var(--blue); }

  ul.tasks{ list-style:none; margin:0; padding:0; }

  .task{
    display:flex;
    align-items:flex-start;
    gap:9px;
    padding:6px 6px;
    border-radius:6px;
    user-select:none;
  }
  .task:hover{ background:rgba(255,255,255,.045); }

  .task-reorder{
    display:flex;
    flex-direction:column;
    gap:2px;
    flex-shrink:0;
    margin-top:1px;
  }
  .mini-reorder-btn{
    width:20px; height:15px;
    background:rgba(255,255,255,.04);
    border:1px solid var(--line);
    color:var(--chalk-dim);
    font-size:.55rem;
    line-height:1;
    border-radius:4px;
    cursor:pointer;
    font-family:inherit;
    padding:0;
  }
  .mini-reorder-btn:hover:not(:disabled){ color:var(--chalk); border-color:rgba(242,237,225,.4); }
  .mini-reorder-btn:disabled{ opacity:.2; cursor:not-allowed; }

  .mark-group{
    display:flex;
    gap:5px;
    flex-shrink:0;
    margin-top:1px;
  }
  .mark-btn{
    width:30px; height:30px;
    border-radius:7px;
    border:1px solid var(--line);
    background:rgba(255,255,255,.03);
    color:var(--chalk-di