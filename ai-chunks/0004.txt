rames celebrationPop{
    0%{ transform:scale(.6); opacity:0; }
    70%{ transform:scale(1.06); opacity:1; }
    100%{ transform:scale(1); }
  }
  .celebration-mascot{
    width:150px;
    height:150px;
    margin:0 auto 10px;
  }
  .celebration-mascot .pet-mascot{ width:100%; height:100%; }
  .celebration-title{
    font-family:"Segoe Print","Bradley Hand",cursive;
    font-size:1.5rem;
    color:var(--yellow);
    margin-bottom:6px;
  }
  .celebration-stage{
    font-size:1rem;
    color:var(--chalk);
    margin-bottom:18px;
  }
  .celebration-stage b{ color:var(--green); }

  footer{
    text-align:center;
    margin-top:22px;
    font-size:.68rem;
    color:rgba(242,237,225,.28);
  }

  /* ---------- editor (modal) ---------- */
  .overlay{
    position:fixed; inset:0;
    background:rgba(10,16,13,.72);
    display:none;
    align-items:flex-start;
    justify-content:center;
    padding:30px 14px;
    overflow-y:auto;
    z-index:100;
  }
  .overlay.open{ display:flex; }

  .editor{
    width:100%;
    max-width:720px;
    background:var(--board);
    border:3px solid var(--wood);
    border-radius:10px;
    padding:20px 20px 24px;
    box-shadow:0 20px 60px rgba(0,0,0,.6);
  }

  .editor-head{
    display:flex;
    align-items:center;
    justify-content:space-between;
    border-bottom:2px dashed var(--line);
    padding-bottom:12px;
    margin-bottom:16px;
  }
  .editor-head h2{
    font-family:"Segoe Print","Bradley Hand",cursive;
    font-weight:normal;
    color:var(--yellow);
    margin:0;
    font-size:1.4rem;
  }

  .editor-section{ margin-bottom:22px; }
  .editor-section h3{
    font-family:"Segoe Print","Bradley Hand",cursive;
    font-weight:normal;
    font-size:1.05rem;
    margin:0 0 4px;
    color:var(--chalk);
  }
  .editor-section .hint{ font-size:.7rem; color:var(--chalk-dim); margin-bottom:10px; }

  .edit-row{
    display:flex;
    align-items:center;
    gap:8px;
    margin-bottom:7px;
  }
  .edit-row-task{
    flex-direction:column;
    align-items:stretch;
    gap:5px;
    padding-bottom:8px;
    margin-bottom:9px;
    border-bottom:1px dashed var(--line);
  }
  .edit-row-task:last-child{ border-bottom:none; }
  .edit-row-main{
    display:flex;
    align-items:center;
    gap:8px;
  }
  .edit-row-main input[type=text]{ flex:1; }
  .reorder-group{ display:flex; flex-direction:column; gap:1px; flex-shrink:0; }
  .reorder-btn{
    width:20px; height:15px;
    font-size:.6rem;
    line-height:1;
    background:rgba(255,255,255,.05);
    border:1px solid var(--line);
    color:var(--chalk-dim);
    cursor:pointer;
    border-radius:3px;
    padding:0;
  }
  .reorder-btn:hover:not(:disabled){ color:var(--chalk); border-color:rgba(242,237,225,.4); }
  .reorder-btn:disabled{ opacity:.25; cursor:not-allowed; }

  .day-chip-row{ display:flex; gap:4px; margin:6px 0; }
  .day-chip{
    width:26px; height:26px;
    border-radius:6px;
    border:1px solid var(--line);
    background:rgba(255,255,255,.04);
    color:var(--chalk-dim);
    font-size:.72rem;
    cursor:pointer;
    font-family:inherit;
  }
  .day-chip.active{
    background:var(--blue);
    border-color:var(--blue);
    color:#16241f;
    font-weight:bold;
  }

  .stepper{
    display:flex;
    align-items:center;
    gap:2px;
    flex-shrink:0;
  }
  .stepper input[type=number]{
    width:44px;
    text-align:center;
    background:rgba(255,255,255,.05);
    border:1px solid var(--line);
    color:var(--chalk);
    border-radius:6px;
    padding:6px 2px;
    font-size:.82rem;
    font-family:inherit;
  }
  .step-btn{
    width:24px; height:28px;
    background:rgba(255,255,255,.05);
    border:1px solid var(--line);
    color:var(--chalk-dim);
    border-radius:6px;
    font-size:.9rem;
    cursor:pointer;
    font-family:inherit;
    line-height:1;
  }
  .step-btn:hover{ color:var(--chalk); border-color:rgba(242,237,225,.4); }

  .sub-input{
    font-size:.74rem !important;
    color:var(--chalk-dim);
    padding:5px 9px !important;
  }
  .edit-row input[type=text]{
    flex:1;
    background:rgba(255,255,255,.05);
    border:1px solid var(--line);
    color:var(--chalk);
    border-radius:6px;
    padding:7px 9px;
    font-size:.82rem;
    font-family:inherit;
  }
  .edit-row input[type=number]{
    width:58px;
    background:rgba(255,255,255,.05);
    border:1px solid var(--line);
    color:var(--chalk);
    border-radius:6px;
    padding:7px 6px;
    font-size:.82rem;
    text-align:center;
    font-family:inherit;
  }
  .edit-row input:focus, .edit-row select:focus{ outline:1px solid var(--yellow); }
  .edit-row select{
    background:rgba(255,255,255,.05);
    border:1px solid var(--line);
    color:var(--chalk);
    border-radius:6px;
    padding:7px 6px;
    font-size:.78rem;
    font-family:inherit;
  }
  .edit-row .del{
    background:none;
    border:none;
    color:var(--red);
    font-size:1rem;
    cursor:pointer;
    padding:2px 6px;
    line-height:1;
  }
  .edit-row .del:hover{ opacity:.7; }

  .add-row-btn{
    background:none;
    border:1px dashed var(--line);
    color:var(--chalk-dim);
    font-size:.78rem;
    padding:7px 12px;
    border-radius:6px;
    cursor:pointer;
    font-family:inherit;
    width:100%;
    margin-top:2px;
  }
  .add-row-btn:hover{ color:var(--green); border-color:var(--green); }

  .editor-footer{
    display:flex;
    justify-content:flex-end;
    gap:10px;
    border-top:2px dashed var(--line);
    padding-top:16px;
    margin-top:6px;
  }

  .btn{
    font-family:inherit;
    font-size:.85rem;
    padding:9px 18px;
    border-radius:8px;
    cursor:pointer;
    border:1px solid var(--line);
    background:none;
    color:var(--chalk-dim);
  }
  .btn:hover{ color:var(--chalk); border-color:rgba(242,237,225,.4); }
  .btn.save{
    background:var(--yellow);
    color:#2a2410;
    border-color:var(--yellow);
    font-weight:bold;
  }
  .btn.save:hover{ opacity:.9; }

  @media (max-width:500px){
    .topbar{ flex-wrap:wrap; }
    .topbar h1{ font-size:1.25rem; }
  }
</style>
</head>
<body>

<span class="points-delta" id="pointsDelta"></span>

<div class="mini-pacus" id="miniPacus"></div>

<div class="overlay" id="evolutionCelebration" style="z-index:700; align-items:center;">
  <div class="celebration-card" id="celebrationCard">
    <div class="celebration-mascot" id="celebrationMascot"></div>
    <div class="celebration-title">🎉 Pacus evoluiu! 🎉</div>
    <div class="celebration-stage" id="celebrationStage"></div>
    <button class="btn save" id="celebrationCloseBtn">continuar</button>
  </div>
</div>

<div class="board-frame">
  <div class="board-inner">

    <div id="testModeBanner"></div>

    <div class="topbar">
      <h1>🥚 Rotina do Hector</h1>
      <div class="topbar-right">
        <div class="pacus-total">
          <span class="label">Pacus Points</span>
          <span class="num" id="totalPoints">0</span>
        </div>
        <button class="icon-btn primary" id="addPointsBtn" title="Ajuste manual de Pacus Points">+ pontos</button>
        <button class="icon-btn primary" id="editBtn">🔒 Área dos adultos</button>
      </div>
    </div>

    <section class="block" id="testPanel" style="display:none;"></section>

    <div class="content">


      <div id="familyGameNightAnnouncement"></div>

      <section class="pet-section" id="petSection"></section>

      <div id="weekendIndicator"></div>
      <div class="periods" id="periods"></div>

      <section class="hair-section" id="hairSection">
        <div class="hair-head">
          <div class="hair-title">🧴 Lavar o cabelo</div>
        </div>
        <div class="hair-actions">
          <button type="button" class="hair-btn" id="hairWashedBtn">💧 Lavei o cabelo</button>
          <button type="button" class="hair-btn" id="hairNotWashedBtn">○ Não lavei</button>
        </div>
        <div class="hair-status" id="hairStatus">Hoje: ainda não registrado</div>
      </section>

      <section class="block" id="gameTimerSection"></section>

      <section class="blo